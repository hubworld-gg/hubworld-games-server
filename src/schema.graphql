extend type Query {
  game(id: ID!): Game
}

type Game {
  id: ID
  name: String
  covers: GameImage
}

type GameImage {
  cover: String
  logo: String
  thumb: String
  screenshotMed: String
  screenshotBig: String
  screenshotHuge: String
  res720: String
  res1080: String
}
